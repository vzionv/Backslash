; Don't fail install when build scripts of un-allowlisted deps are skipped.
; The allowlist in pnpm-workspace.yaml still controls which packages may run
; their scripts; this just downgrades the "ignored builds" check from fatal
; to a warning so deploys don't break every time a new transitive dep ships a
; postinstall hook.
strict-dep-builds=false
